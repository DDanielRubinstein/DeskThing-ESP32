#include <iostream>
#include <string.h>

using std::string;

namespace env {
    // wifi creds
    extern string ssid;
    extern string pass;
    // spotify creds
    extern string client_id;
    extern string client_secret;
}